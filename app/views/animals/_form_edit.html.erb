<%= form_for @animal, html: { multipart: true } do |f| %>
<!-- <% if @animal.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h2>
    <ul>
        <% @animal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
        <% end %>
    </ul>
</div>
<% end %> -->

<div class="field">
    <%= label_tag "Nombre del animal" %><br>
    <%= text_field_tag :title, "#{@animal.title}" %>
</div>
<div class="field">
    <%= label_tag "El animal es un" %><br>
    <%= select_tag  :breed, options_for_select([["Perro", "Dog"], ["Gato", "Cat"]]) %>
</div>
<div class="field">
    <%= label_tag "Historia o descripción" %><br>
    <%= text_area_tag :description, "#{@animal.description}" %>
</div>
<div class="field">
    <%= label_tag "Foto" %><br>
    <%= file_field_tag :photos  %>
</div>
<div class="field">
    <%= label_tag "Provincia" %><br>
    <%= select_tag "location_id", options_from_collection_for_select(@locations, "id", "name") %>
</div>
<div class="actions">
    <%= f.submit "Guardar" %>
</div>
<% end %>
